version: "1.29.2"

services:
  nginx:
    build: ./requierments/nginx
    ports:
      - 443:443
  
  wordpress:
    build: ./requierments/wordpress
    ports:
    - 9000:9000

  mariadb:
    build: ./requierments/mariadb
    ports:
      -3306